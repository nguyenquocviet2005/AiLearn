# Frontend-only design prototype

Route: **`/prototype`**

Guided closed-loop walkthrough with **mock data only** (no `fetch`, no API):

1. Intro
2. Teacher class overview (40 synthetic learners)
3. Lesson plan edit / approve / publish (in-memory versions)
4. Student readiness + remediation (`StudentPreviewWorkspace` personas)
5. Intervention report (+ printable)

Open via landing CTA **“Xem prototype thiết kế”** or go directly to `/prototype`.
