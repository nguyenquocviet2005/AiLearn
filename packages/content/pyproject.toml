[project]
name = "ailearn-content"
version = "0.1.0"
description = "AiLearn template-based content service with optional LLM fallback"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ailearn_content"]
