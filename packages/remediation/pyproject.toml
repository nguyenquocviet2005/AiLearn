[project]
name = "ailearn-remediation"
version = "0.1.0"
description = "AiLearn remediation state machine and path selection"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ailearn_remediation"]
